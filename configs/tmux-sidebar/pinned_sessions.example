# tmux-sidebar の上部に固定するセッション名（1 行 1 セッション）
# 上から順にサイドバー上部に並ぶ。並びは tmux の session 列挙順より優先される。
# ~/.config/tmux-sidebar/pinned_sessions にコピーして編集する。
#
# pin した session は削除保護も兼ねる:
#   - D（session kill）と d（最後の window 削除）はブロックされる
#   - 解除は本ファイルから該当行を削除してから再度 kill する
#
# 例:
# infra
# work
# tmux-sidebar
